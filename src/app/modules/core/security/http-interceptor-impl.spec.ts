import { HttpInterceptorImpl } from './http-interceptor-impl';

describe('HttpInterceptorImpl', () => {
  it('should create an instance', () => {
    expect(new HttpInterceptorImpl()).toBeTruthy();
  });
});
